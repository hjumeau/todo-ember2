{{todo-header}}
<section class="main">
    <ul class="todo-list">
        {{#each todos as |todo|}}
            {{todo-item todo=todo}}
        {{/each}}
    </ul>
</section>
{{#if todos}}
    {{todo-footer}}
{{/if}}