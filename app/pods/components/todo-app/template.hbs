{{todo-header createTodo=(action 'createTodo')}}
<section class="main">
    <ul class="todo-list">
        {{#each todos as |todo|}}
            {{todo-item todo=todo toggleTodo=(action 'toggleTodo')}}
        {{/each}}
    </ul>
</section>
{{#if todos}}
    {{todo-footer}}
{{/if}}